export interface Service {
  title: string;
  description: string;
}

export interface Testimonial {
  author: string;
  content: string;
}

export interface ContactForm {
  name: string;
  email: string;
  message: string;
}